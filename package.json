{
  "name": "node-process-monitor",
  "version": "1.0.0",
  "description": "Get process information in real time",
  "main": "./dist/index.js",
  "repository": {
    "type": "git",
    "url": "git@github.com:xiaobaidadada/node-process-monitor.git"
  },
  "scripts": {
    "install": "prebuild-install --verbose || node-gyp rebuild --release",
    "postinstall": "npx tsc --build",
    "cc-rebuild": " node-gyp rebuild",
    "cc-configure": "node-gyp configure",
    "go": "npm run cc-configure && node-gyp build ",
    "test": "mocha --require ts-node/register --ui bdd ./test/**"
  },
  "keywords": [],
  "author": "xiaobaidadada",
  "license": "MIT",
  "dependencies": {
    "prebuild-install": "^7.1.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.9",
    "@types/node": "^22.7.5",
    "bindings": "^1.5.0",
    "mocha": "^10.7.3",
    "node-addon-api": "^7.0.0",
    "node-gyp": "^10.2.0",
    "prebuild": "^13.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
